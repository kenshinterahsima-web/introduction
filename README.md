# kenshin-site

`HTML` / `CSS` / `JavaScript` だけで作ったシンプルな静的サイトです。

## Preview

```bash
npm run dev
```

ブラウザで `http://localhost:4173` を開いて確認できます。

## Files

- `index.html` : 画面の土台
- `styles.css` : 全体スタイル
- `script.js` : 画面描画とルーティング（`#home`, `#favorites`, `#favorites/:slug`）
